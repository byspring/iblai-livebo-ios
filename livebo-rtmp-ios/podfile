# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'livebo-rtmp-ios' do
  # Comment the next line if you don't want to use dynamic frameworks
  #use_modular_headers!
  use_frameworks!
  # Pods for livebo-rtmp-ios

  target 'livebo-rtmp-iosTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'livebo-rtmp-iosUITests' do
    # Pods for testing
  end

  
  pod 'AFNetworking', '~> 4.0.1'
  pod 'IQKeyboardManager', '~> 6.5.0'
  pod 'MJExtension'
  pod 'MJRefresh', '~> 3.4.3'
  pod 'SDWebImage'
  pod 'WMPageController'
  pod 'QMUIKit', '~> 4.1.3'
  pod 'FDFullscreenPopGesture'
  pod 'HXPhotoPicker'
  pod 'BRPickerView'
  pod 'LFLiveKit'

end
